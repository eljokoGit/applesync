[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "applesync"
dynamic = ["version"]
description = "Sauvegarde vérifiable des photos et vidéos d'un iPhone vers un dossier local, sans conversion, via le protocole Apple (usbmuxd/AFC)."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
keywords = ["iphone", "backup", "photos", "afc", "usbmuxd", "heic", "windows"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Win32 (MS Windows)",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: French",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3.12",
    "Topic :: System :: Archiving :: Backup",
]
dependencies = [
    # Version FIGÉE : applesync/device/afc.py utilise l'API interne
    # _send_and_wait pour implémenter FILE_SEEK (absent de l'API publique).
    # Toute montée de version doit re-valider la reprise de copie.
    "pymobiledevice3==10.3.1",
    "PySide6>=6.6",
    "Pillow>=10.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
applesync = "applesync.__main__:main"

[project.gui-scripts]
applesync-gui = "applesync.__main__:main"

[project.urls]
Homepage = "https://github.com/eljokoGit/applesync"
Issues = "https://github.com/eljokoGit/applesync/issues"
Changelog = "https://github.com/eljokoGit/applesync/blob/main/CHANGELOG.md"

[tool.setuptools.dynamic]
version = { attr = "applesync.__version__" }

[tool.setuptools.packages.find]
include = ["applesync*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
